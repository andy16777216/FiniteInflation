#include <iostream>
int main()
{
    using namespace std;
    cout << "Hello, world!" << endl;
    return 0;
}

#include <iostream>
#include <boost/math/special_functions/bessel.hpp>

int main(int argc, char *argv[])
{
  using namespace std;

  double kmin = 1.E-6;
  double kmax = 1.;

  double D10 = 1.0*atof(argv[1]);
  double D20 =  


  double D10 = 1.0*atof(argv[1]);
  double D20 = 1.0e-3*atof(argv[2]);
  double logkc = -1.0*atof(argv[3]);


cyl_bessel_j(v, x) = Jv(x)

cyl_neumann(v, x) = Yv(x) = Nv(x)





return 0;
}
